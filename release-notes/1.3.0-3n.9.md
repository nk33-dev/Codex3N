## Codex3N 1.3.0-3n.9

- 修正模型菜单排序：官方目录和补充型号统一按名称与版本排列，新增高版本不再被旧条目压到后面。
- Mobile relay 默认只监听本机；通过首帧注册、服务端签发的十分钟令牌、重放保护、常量时间比较和连接限流保护会话。
- 管理器启用 WebView CSP、缩小文件对话框权限和外链站点白名单，二维码改以图片加载。
- PR 与发布增加 relay 模块 Clippy、Rust 和 npm 依赖安全审计；更新受影响的锁文件依赖。
- 小范围拆分管理器路由文案与外链命令，保持原入口。
- 修复更新版 Clippy 下的 relay 构建兼容性。
